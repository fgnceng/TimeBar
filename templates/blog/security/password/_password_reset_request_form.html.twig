{{ form_start(form, {'attr': {'id': 'password-reset-request-form'}}) }}
    <div class="field">
        <p class=" subtitle is-6 has-text-left">Fill in the fields with the email address of your account to receive a reset link: </p>
        {{ form_widget(form.email, {'attr': {'class': 'input'}}) }}
        <div class="help is-danger has-text-left">
            {{ form_errors(form.email) }}
            {{ form_errors(form) }}
        </div>
    </div>

    <button type="submit" class="button is-primary">Send email</button>
{{ form_end(form) }}
